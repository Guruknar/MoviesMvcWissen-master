@model Movie

@using _036_MoviesMvcWissen.Helpers

@{
    ViewBag.Title = "Edit Movie";
    var years = ViewBag.Years as SelectList; /*Checkkk*/
    var directors = (MultiSelectList)ViewBag.directors;
}

<h2>@ViewBag.Title</h2>

@*@Html.BeginForm("Edit", "Movies", FormMethod.Post)
    {

    }
    @{
        Html.EndForm();
    }*@

@using (Html.BeginForm("Edit", "Movies", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.Hidden("Id", Model.Id)
    @*<input type="hidden" name="Id" value="@Model.Id" />*@
    <div class="form-group">
        @Html.Label("tbName", "Name:  ")&emsp;&emsp;&emsp;&emsp;&emsp;&nbsp;
        @Html.TextBox("Name", Model.Name, new { @class = "form-control-sm", placeholder = "Enter movie name", id = "tbName" })
    </div>
    <div class="form-group">
        @Html.Label("ddlProductionYear", "Production Year:")&emsp;
        @Html.DropDownList("ProductionYear", years, new { @class = "form-control-sm", id = "ddlProductionYear" })
    </div>
    <div class="form-group">
        @Html.Label("tbBoxOfficeReturn", "Box Office Return:")&nbsp;
        @Html.TextBox("BoxOfficeReturn", Model.BoxOfficeReturn, new { @class = "form-control-sm",
       placeholder = "Enter movie box office return", id = "tbBoxOfficeReturn" })
    </div>
    @*<div class="form-group"> ilk yol
            <label for="ddlDirectos">Directors</label>
            <select id="ddlDirectos" class="form-control" name="Directors" multiple>
                @foreach (var director in directors)
                {
                    <option value="@director.Value">@director.Text</option>
                }
            </select>
        </div>*@
    <div class="form-group">
        @Html.Label("ddlDirectors", "Directors")
        @Html.ListBox("directorIds", directors, new { @class = "form-control", id = "ddlDirectors" })
    </div>

    <div class="form-group">
        @Html.Label("fuImage", "Image:")
        <br />
        <input type="file" name="Image" accept="image/*" id="fuImage" />
    </div>


    @*<input type="submit" class="btn btn-success" value="Save" />*@
    @Html.SaveButton()
    @:&nbsp;

    @*<input type="reset" class="btn btn-warning" value="Clear" />*@

    @Html.Button("Clear", "btn btn-warning", "reset")


}



